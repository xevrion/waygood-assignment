{
  "name": "waygood-ai-task-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "dev": "docker compose up --build",
    "test": "npm run test --workspaces --if-present"
  }
}

