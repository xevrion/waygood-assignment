apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: waygood-platform
  namespace: waygood-ai
spec:
  ingressClassName: nginx
  rules:
    - host: waygood.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 4000

